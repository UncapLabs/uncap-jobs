{
	"name": "uncap-jobs",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler types && wrangler deploy",
		"dev": "wrangler dev --test-scheduled",
		"dev:shared": "wrangler dev --test-scheduled --persist-to ../bitusd-app/.wrangler/state",
		"start": "wrangler dev --test-scheduled",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"big.js": "^7.0.1",
		"drizzle-orm": "^0.33.0",
		"starknet": "^9.2.1",
		"viem": "^2.43.5"
	},
	"devDependencies": {
		"@types/big.js": "^6.2.2",
		"@types/node": "^25.0.7",
		"typescript": "^5.5.2",
		"wrangler": "^4.43.0"
	}
}
